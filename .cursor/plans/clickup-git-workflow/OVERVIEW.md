# ClickUp and Git work workflow

Short overview: document and automate the grilled ClickUp ↔ Git ladder (intake → grill → branch → PRD → commit-able slices → PR to `staging`), including short path and mid-task interrupt rules.

**PRD:** `.cursor/plans/clickup-git-workflow.prd.md`

Vertical slices live in `issues/` (commit-able under `.cursor/plans/`, not `.scratch/`).

**Related:** `/start-work` (orchestrator: full path, short path, interrupt, ready-PR, post-merge), `/ask-matt`, `/grill-me`, `/to-prd`, `/to-issues`, `.cursorrules` ClickUp config
