# Report primary independent of column order

Make report grain (primary table) explicit and independent of column order, show nested to-many Invoice dates on Customer-primary reports, and repair stuck configs.

**PRD:** `.cursor/plans/report-primary-independent-of-column-order.prd.md`

**Related:** formula comparisons blank-date diagnosis; existing `resolveReportTablesPreservingPrimary` preserve-primary helper.

Vertical slices live in `issues/`.
