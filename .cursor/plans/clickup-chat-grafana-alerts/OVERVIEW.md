# ClickUp Chat Grafana Alerts

Production Grafana ops alerts post as markdown Chat **messages** (not tasks) into the existing **ARchaser** ClickUp Chat channel. Email and Slack stay on. Staging stays silent.

**PRD:** `.cursor/plans/clickup-chat-grafana-alerts.prd.md`

Vertical slices live in `issues/` (commit-able under `.cursor/plans/`, not `.scratch/`).

**Related:** Grafana alert routing optimization (silent staging, critical vs digest email — do not reopen)
