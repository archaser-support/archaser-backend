{
  "name": "@archaser/worker",
  "version": "0.0.1",
  "private": true,
  "description": "Archaser BullMQ worker (Stage 2 peel — in-repo scaffold)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "start:dev": "ts-node -r tsconfig-paths/register src/main.ts",
    "test": "echo \"worker: add consumer tests in peel CI\""
  },
  "dependencies": {
    "@archaser/billing-connector": "*",
    "@archaser/cron-jobs": "*",
    "@archaser/database": "*",
    "@nestjs/common": "^11.1.3",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.1.3",
    "bullmq": "^5.81.3",
    "ioredis": "^5.4.2",
    "prom-client": "^15.1.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
