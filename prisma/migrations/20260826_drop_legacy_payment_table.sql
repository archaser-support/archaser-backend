-- Companion DDL for drop-legacy-payment-table.ts
-- Prefer running the TS script (rewrites reports + strips generic_field_config.payment).
-- This file documents the final DDL only. Do not run alone if Payment-table reports remain.

-- SELECT COUNT(*) FROM "Payment";
-- DROP TABLE IF EXISTS "Payment";
